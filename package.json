{
  "name": "anvil-zksync-action",
  "version": "1.0.0",
  "description": "GitHub Action to run anvil-zksync with various configurations.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build index.js --outdir dist --minify --target node"
  },
  "keywords": [
    "github",
    "action",
    "anvil-zksync",
    "zksync",
    "blockchain"
  ],
  "author": "Dustin Brickwood",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.1",
    "axios": "^1.7.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
